 <div>
      <div className="breadcrumbs">
        {clickedFileBreadcrumbs.map((folderName, index) => (
          <span key={index}>{folderName} / </span>
        ))}
      </div>
      <ul className="file-list">
        {filesData.map((file) => (
          <li key={file.id} onClick={() => handleFileClick(file)}>
            {file.name}
          </li>
        ))}
      </ul>
    </div>
