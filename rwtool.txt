 <div>
        {clickedFileBreadcrumbs && (
          <Breadcrumbs aria-label="breadcrumb">
            {Object.entries(clickedFileBreadcrumbs).map(([index, breadcrumb]) => (
              <Typography key={index} color="textPrimary">
                {breadcrumb}
              </Typography>
            ))}
          </Breadcrumbs>
        )}
      </div>
