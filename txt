import React,{useState} from "react";
import { Container, Paper, TextField, Button, Grid } from "@material-ui/core";
import FolderIcon from "@material-ui/icons/Folder";
import FileIcon from "@material-ui/icons/InsertDriveFile";

const MainContent = ({folders}) => {
  const [currentFolders, setCurrentFolders] = useState(folders);
  const [breadcrumbs, setBreadcrumbs] = useState([]);

  const handleFolderClick = (clickedFolder) => {
    setBreadcrumbs([...breadcrumbs, clickedFolder.name]);
    setCurrentFolders(clickedFolder.subfolders);
  };
  return (
    <Container>
      <Paper elevation={3} style={{ padding: "20px", marginBottom: "20px" }}>
      {breadcrumbs.map((folderName, index) => (
          <span key={index}>
            {folderName}
            {index !== breadcrumbs.length - 1 && " > "}
          </span>
        ))}
        {/* Breadcrumbs will go here */}
      </Paper>
      {/* <Paper elevation={3} style={{ padding: "20px", marginBottom: "20px" }}> */}
        {/* Search bar and filters will go here */}
      {/* </Paper> */}
      <Grid container spacing={3}>
        {folders.map((folder, index) => (
          <Grid item xs={3} key={index} onClick={() => handleFolderClick(folder)}>
          <FolderIcon />
          <span>{folder.name}</span>
        </Grid>
        ))}
      </Grid>
    </Container>
  );
};

export default MainContent;

import React from "react";
import { Breadcrumbs as MUIBreadcrumbs, Link, Typography } from "@material-ui/core";

const Breadcrumbs = ({ breadcrumbs }) => {
  return (
    <MUIBreadcrumbs aria-label="breadcrumb">
      {breadcrumbs.map((breadcrumb, index) => (
        <Link color="inherit" href="#" key={index}>
          {breadcrumb}
        </Link>
      ))}
    </MUIBreadcrumbs>
  );
};

export default Breadcrumbs;

// import Header from "./components/frontend/Header.js";
// import User from "./components/frontend/User.js";
// function App() {
//   return (
//     <div>
//       <div>
//         <Header />
//       </div>
//       <div>
//         <User />
//       </div>
//     </div>
//   );
// }

// export default App;
import React from "react";
import { CssBaseline, Grid } from "@material-ui/core";
import { makeStyles } from "@material-ui/core/styles";
import AppBarComponent from "./components/frontend/AppBar";
import Sidebar from "./components/frontend/Sidebar";
import Breadcrumbs from "./components/frontend/Breadcrumbs";
import MainContent from "./components/frontend/MainContent";
import ProfileMenu from "./components/frontend/ProfileMenu";

const useStyles = makeStyles((theme) => ({
  root: {
    display: "grid",
    gridTemplateColumns: "auto 1fr",
  },
}));


function App() {
  const classes = useStyles();

  const breadcrumbs = ["Home", "Folder Name"];
  const initialFolders = [
    {
      name: "Folder A",
      subfolders: [
        {
          name: "Subfolder A1",
          subfolders: [],
        },
        {
          name: "Subfolder A2",
          subfolders: [
            {
              name: "Sub-subfolder A2.1",
              subfolders: [],
            },
          ],
        },
      ],
    },
    {
      name: "Folder B",
      subfolders: [
        {
          name: "Subfolder B1",
          subfolders: [],
        },
      ],
    },
    {
      name: "Folder C",
      subfolders: [],
    },
  ];

  return (
    <div>
       <CssBaseline />
      <AppBarComponent />
    <div className={classes.root}>
     
      <Sidebar />
      <main>
        <Breadcrumbs breadcrumbs={breadcrumbs} />
        <MainContent folders={initialFolders}/>
      </main>
    </div>
    </div>
  );
}

export default App;
