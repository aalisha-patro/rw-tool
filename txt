 <Grid container spacing={3}>
        {data.map((item, index) => (
          <Grid item xs={3} key={index}>
            {item.type === "folder" ? (
              <FolderIcon />
            ) : (
              <FileIcon />
            )}
            <span>{item.name}</span>
          </Grid>
        ))}
      </Grid>
