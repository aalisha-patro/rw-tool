import React from 'react';
import File from './File';

function MiddleSection({ files }) {
  return (
    <div className="middle-section">
      {files.map(file => (
        <File key={file.id} name={file.name} />
      ))}
    </div>
  );
}

export default MiddleSection;
